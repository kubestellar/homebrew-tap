__pycache__/

# Scratch PR-description drafts (e.g. from `gh pr create --body-file`) should
# never be committed — see issue #360.
pr-body.txt
*.pr-body.txt

# Coverage artifacts (generated by pytest --cov in CI)
.coverage
.coverage.*
coverage.xml
htmlcov/
