name: Feedback Wanted

on:
  pull_request:
    types: [closed]

permissions: {}

jobs:
  feedback:
    permissions:
      contents: read  # needed: read repository context
      pull-requests: write  # needed: comment on pull requests
    uses: kubestellar/infra/.github/workflows/reusable-feedback.yml@1a04a3fd17771e78e5cf6d42a3fda84e3ba478f3  # main@2026-06-24
    secrets: inherit
