name: Label Helper

on:
  issue_comment:
    types: [created]

permissions: {}

jobs:
  label-helper:
    permissions:
      contents: read  # needed: read repository context
      issues: write  # needed: manage issue labels
      pull-requests: write  # needed: manage PR labels
    uses: kubestellar/infra/.github/workflows/reusable-label-helper.yml@1a04a3fd17771e78e5cf6d42a3fda84e3ba478f3  # main@2026-06-24
    secrets: inherit
